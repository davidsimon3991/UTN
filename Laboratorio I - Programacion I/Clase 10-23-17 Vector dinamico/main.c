#include <stdio.h>
#include <stdlib.h>
#include "Prototipos.h"

int main()
{
    //eRecursante* recursante;
    /*
    printf("Ingrese legajo:");
    scanf("%d",&legajo);
    printf("Ingrese nombre:");
    fflush(stdin);
    gets(nombre);
    printf("Ingrese nota:");
    scanf("%d",&nota);
    recursante = newRecursante_info(legajo,nombre,nota);*/

    eRecursante * lista[3];
    eRecursante*aux;

    altaRecursante(aux,3);


    return 0;
}
